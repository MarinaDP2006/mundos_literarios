import { CanActivateFn } from '@angular/router';

/**
 * Guard funcional de autenticación.
 *
 * Placeholder de arquitectura: la implementación completa (que inyecta
 * AuthService y redirige a /login si no hay sesión) se genera en la
 * Fase 3 (Firebase), junto con AuthService y los signals de sesión.
 */
export const authGuard: CanActivateFn = () => {
  // Implementación completa en Fase 3 (Firebase / Auth).
  return true;
};
